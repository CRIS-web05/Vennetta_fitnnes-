{
  "buildCommand": "cd cliente && npm install && CI=false npm run build",
  "outputDirectory": "cliente/build",
  "env": {
    "CI": "false"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
